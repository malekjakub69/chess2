<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>
			{% block title %}Welcome!
			{% endblock %}
		</title>

		{% block javascripts %}
			{% block importmap %}
				{{ importmap('app') }}
			{% endblock %}
		{% endblock %}

		<style>
			button {
				padding: 6px 12px;
				border-radius: 12px;
				text-align: center;
				display: inline-block;
			}
		</style>
	</head>
	<body>
		<a href="/">Domů</a>
		<h1>Uživatel
			{{user.name}}</h1>
		<div>
			Email :
			{{user.email}}
		</div>
		<div>
			Telefon :
			{{user.phone}}
		</div>
		<div>
			Výhry :
			{{user.getWhiteWin + user.getBlackWin}}
			( B:{{user.getWhiteWin}}
			/ Č:{{user.getBlackWin}})
		</div>
		<div>
			Prohry :
			{{user.getWhiteLose + user.getBlackLose}}
			( B:{{user.getWhiteLose}}
			/ Č:{{user.getBlackLose}})
		</div>
		<div>
			Nejlepší hra :
			{{user.getBestGame}}
		</div>
		<button onclick="location.href = '/user/{{user.id}}/edit';">
			Upravit
		</button>
	</body>
</html>
